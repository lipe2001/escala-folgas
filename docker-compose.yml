services:
  escalas_api:
    build: .
    container_name: escalas-noc-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    dns:
      - 8.8.8.8
      - 1.1.1.1
  